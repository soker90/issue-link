---
import { Icon } from 'astro-icon/components';
import { getPermalink } from '~/utils/permalinks';
import Button from '../ui/Button.astro';
export interface Props {
  items: Array<String>;
  title: String;
}

const { items, title } = Astro.props;
---

<div class="flex flex-col gap-4 lg:p-4 p-2 rounde-lg m-2">
  <div class="mt-2 md:mt-3 mx-auto text-xl text-muted font-medium">{title}</div>

  {
    items.map((item) => (
      <Button
        variant="secondary"
        class="md:p-4 px-3 flex justify-between no-underline border-2 border-hairline hover:border-muted transition-all"
        href={item}
        icon="tabler:chevron-right"
      >
        {item}
      </Button>
    ))
  }
</div>
